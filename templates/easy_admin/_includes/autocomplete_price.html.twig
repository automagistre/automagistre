<script>
    var priceInput = $('#{{ id }}');

    $('[data-easyadmin-autocomplete-url]').on("select2:selecting", function (e) {
        var price = e.params.args.data.price;

        if (price) {
            priceInput.val(e.params.args.data.price);
        }
    });
</script>
