Заказывать
до количества <strong>{{ item.orderUpToQuantity / 100 }}</strong>
если количество ниже <strong>{{ item.orderFromQuantity / 100 }}</strong>

<div class="pull-right">
    <a class="btn btn-xs btn-info" href="{{ easyadmin_path('PartRequiredAvailability', 'new', {'part_id': item|toId}) }}">
        Изменить
    </a>
</div>
