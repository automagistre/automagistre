table:
    name: contact_type
    schema: public
insert_permissions:
    -   permission:
            backend_only: false
            check: { }
            columns:
                - id
                - name
        role: manager
select_permissions:
    -   permission:
            allow_aggregations: true
            columns:
                - id
                - name
            filter: { }
        role: manager
update_permissions:
    -   permission:
            check: null
            columns:
                - id
                - name
            filter: { }
        role: manager
delete_permissions:
    -   permission:
            filter: { }
        role: manager
