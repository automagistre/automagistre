table:
    name: vehicle_air_intake
    schema: public
select_permissions:
    -   permission:
            allow_aggregations: true
            columns:
                - id
                - name
            filter: { }
        role: manager
