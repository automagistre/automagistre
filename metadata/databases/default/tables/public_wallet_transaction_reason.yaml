table:
    name: wallet_transaction_reason
    schema: public
is_enum: true
select_permissions:
    -   permission:
            columns:
                - id
                - name
            filter: { }
        role: manager
