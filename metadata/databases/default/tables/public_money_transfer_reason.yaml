table:
    name: money_transfer_reason
    schema: public
select_permissions:
    -   permission:
            columns:
                - id
                - name
            filter: { }
        role: manager
