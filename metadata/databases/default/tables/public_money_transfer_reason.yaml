table:
    name: money_transfer_reason
    schema: public
is_enum: true
select_permissions:
    -   permission:
            columns:
                - id
                - name
            filter: { }
        role: manager
