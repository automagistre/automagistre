framework:
    test: ~
    session:
        storage_factory_id: session.storage.factory.mock_file
    profiler:
        collect: false

parameters:
    env(APP_SECRET): ThisIsVerySuperSecretVariableForTests
